<tr>
    <td><%= produto.nome %></td>
    <td><%= produto.descricao %></td>
    <td><%= produto.preco %></td>
    <td><%=link_to "Alterar", edit_produto_path(produto),method: :get, class: "btn btn-defautl"%></td>
    <td><%= button_to "Remover",produto,
        method: :delete,
        class: "btn btn-danger",
        data: {confirm: "Tem certeza que deseja remover #{produto.nome}?"} %>
   </td>
</tr>
